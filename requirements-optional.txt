# JARVIS v2 Optional Dependencies
# These are heavy packages — only needed for advanced features.
# Install separately: pip install -r requirements-optional.txt

# Pattern recognition & embedding generation
sentence-transformers>=2.2
numpy>=1.24

# Local whisper transcription (if not using NVIDIA Whisper API)
# openai-whisper>=20231117

# Document generation — Excel and PPT creation from JARVIS value data / battlecards
# Used when Claude creates Excel ROI models or PPT battlecard decks from value_data.json / battlecard_data.json
python-pptx>=0.6.21
openpyxl>=3.1.0
